<%- include("partials/header") -%>

<div id="last-child">
    <h1>Home</h1>
    <p><%=startingContent%></p>
    <!-- <% for(var i=0; i<posts.length; i++){ %>
        <h1><%=posts[i].title%></h1>
        <p><%=posts[i].content%></p>
    <% }; %> -->
    <a href="/compose"><button type="submit" name="button" class="btn btn-primary">Compose</button></a>
    <% posts.forEach(function(post){ %>
        <h1><%=post.title%></h1>
        <p>
            <%=post.content.substring(0,100)+" ..."%>
            <a href="/posts/<%=post.kebabTitle%>"> Read More</a> 
        </p>
    <% }); %>
</div>

<%- include("./partials/footer.ejs") -%>